<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
        <a class="navbar-brand fw-bold" href="/">Kuky's News 📰</a>
        <div class="collapse navbar-collapse">
            <ul class="navbar-nav ms-auto">
                {{#if user}}
                    <li class="nav-item">
                        <span class="nav-link text-info fw-bold">Hola, {{user.nombre}}</span>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-danger" href="/auth/logout">Cerrar Sesión</a>
                    </li>
                {{else}}
                    <li class="nav-item">
                        <a class="nav-link" href="/auth/login">Login</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-warning" href="/auth/register">Registro</a>
                    </li>
                {{/if}}
            </ul>
        </div>
    </div>
</nav>